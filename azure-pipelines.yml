# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'
- task: TerraformTaskV1@0
  inputs:
    provider: 'azurerm'
    command: 'init'
    backendServiceArm: 'SPHotmailaccount'
    backendAzureRmResourceGroupName: 'terraform-backed-rg'
    backendAzureRmStorageAccountName: 'storageaccchanterraform'
    backendAzureRmContainerName: 'terraformteststate'
    backendAzureRmKey: 'terraformtest.tfstate'
